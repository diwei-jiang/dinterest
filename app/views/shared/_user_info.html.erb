<%= link_to gravatar_for(current_user), current_user %>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "view my profile", current_user %>
</span><br>
<span>
  <%= pluralize(current_user.pins.count, "pin") %>
</span>
<span>
  <%= pluralize(current_user.boards.count, "board") %>
</span>