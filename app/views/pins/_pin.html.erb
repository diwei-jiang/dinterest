<li>
  <span class="content">
  <%= link_to pin.id, pin_path(pin.id) %>
  <% if current_user?(pin.user) %>
    <%= link_to "delete", pin, method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: pin.description %>
  <% end %>
  </span>
</li>